# Base URL of the OpenLake backend, e.g. http://localhost:9000
# Leave empty to call same-origin (useful if you proxy /openlake and
# bucket routes through Vite's dev server - see vite.config.js).
VITE_OPENLAKE_BASE_URL=http://localhost:9000
